# todo-app-api-mern
This project is the backend API for a MERN-based To-Do application. It provides RESTful endpoints to manage tasks efficiently , including creating, reading, updating, and deleting (CRUD) operations. The API is designed with scalability and modularity in mind, serving as the core for the frontend To-Do client application.
